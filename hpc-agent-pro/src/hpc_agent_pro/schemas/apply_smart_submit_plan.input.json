{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/apply_smart_submit_plan.input.json",
  "title": "apply-smart-submit-plan input spec",
  "type": "object",
  "required": [
    "submit_spec",
    "score_submit_plan_envelope"
  ],
  "additionalProperties": false,
  "properties": {
    "submit_spec": {
      "additionalProperties": true,
      "description": "The base submit spec the agent has built so far — a dict matching ``SubmitFlowSpec``. The workflow returns it refined in place; the original is not mutated.",
      "title": "Submit Spec",
      "type": "object"
    },
    "score_submit_plan_envelope": {
      "additionalProperties": true,
      "description": "The verbatim ``data`` field of a prior ``score-submit-plan`` (a.k.a. ``plan-submit``) envelope. Field names track :class:`PlanSubmitResult` — see ``hpc_agent_pro._wire.queries.plan_submit``.",
      "title": "Score Submit Plan Envelope",
      "type": "object"
    }
  }
}
