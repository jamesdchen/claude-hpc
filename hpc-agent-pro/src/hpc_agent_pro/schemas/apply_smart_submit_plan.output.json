{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/apply_smart_submit_plan.output.json",
  "title": "apply-smart-submit-plan output data",
  "type": "object",
  "required": [
    "refined_spec"
  ],
  "additionalProperties": false,
  "properties": {
    "refined_spec": {
      "additionalProperties": true,
      "description": "The submit spec with auto-applied refinements layered in.",
      "title": "Refined Spec",
      "type": "object"
    },
    "decisions": {
      "description": "One entry per planner signal the workflow acted on (or escalated). Shape: ``{point, outcome, why, ...}``. ``outcome='pending'`` marks an escalation that the caller must confirm before proceeding.",
      "items": {
        "additionalProperties": true,
        "type": "object"
      },
      "title": "Decisions",
      "type": "array"
    }
  }
}
